<?xml version="1.0" encoding="utf-8" ?>
<components:CustomContentPage
    x:Class="MoMoney.Views.Stats.InsightsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:chart="clr-namespace:Syncfusion.Maui.Toolkit.Charts;assembly=Syncfusion.Maui.Toolkit"
    xmlns:converters="clr-namespace:MoMoney.Converters"
    xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:components="clr-namespace:MoMoney.Components"
    xmlns:viewmodel="clr-namespace:MoMoney.Core.ViewModels.Stats;assembly=MoMoney.Core"
    HeaderTitle="Insights"
    x:DataType="viewmodel:InsightsViewModel">
    <components:CustomContentPage.Resources>
        <converters:ShowValueConverter x:Key="ShowValueConverter" />
    </components:CustomContentPage.Resources>

    <Grid
        Padding="10"
        RowDefinitions="Auto,Auto,*,Auto,Auto"
        RowSpacing="10">

        <material:PickerField
            Title="Select a Year"
            AllowClear="False"
            Icon="{FontImageSource FontFamily=MaterialRoundedFilled,
                                Glyph={x:Static m:MaterialOutlined.Calendar_month}}"
            ItemsSource="{Binding Years}"
            SelectedItem="{Binding SelectedYear}"
            SelectedValueChangedCommand="{Binding RefreshCommand}"
            WidthRequest="120" />

        <Label
            Grid.Row="1"
            Style="{StaticResource lblTitle}"
            Text="Income &amp; Expenses" />
        <Border Grid.Row="2" Padding="0">
            <chart:SfCartesianChart
                Margin="0,0,0,10"
                EnableSideBySideSeriesPlacement="True"
                IsTransposed="True">
                <chart:SfCartesianChart.ZoomPanBehavior>
                    <chart:ChartZoomPanBehavior EnablePanning="True" ZoomMode="XY" />
                </chart:SfCartesianChart.ZoomPanBehavior>

                <chart:SfCartesianChart.XAxes>
                    <chart:CategoryAxis
                        Interval="1"
                        IsInversed="True"
                        ShowMajorGridLines="False">
                        <chart:CategoryAxis.AxisLineStyle>
                            <chart:ChartLineStyle Stroke="{AppThemeBinding Light={StaticResource outlineVariant}, Dark={StaticResource secondaryContainerDark}}" />
                        </chart:CategoryAxis.AxisLineStyle>
                        <chart:CategoryAxis.MajorTickStyle>
                            <chart:ChartAxisTickStyle TickSize="0" />
                        </chart:CategoryAxis.MajorTickStyle>
                        <chart:CategoryAxis.LabelStyle>
                            <chart:ChartAxisLabelStyle TextColor="{AppThemeBinding Light={StaticResource onSurface}, Dark={StaticResource onSurfaceDark}}" />
                        </chart:CategoryAxis.LabelStyle>
                    </chart:CategoryAxis>
                </chart:SfCartesianChart.XAxes>
                <chart:SfCartesianChart.YAxes>
                    <chart:NumericalAxis IsVisible="False" RangePadding="AppendInterval">
                        <chart:NumericalAxis.MajorGridLineStyle>
                            <chart:ChartLineStyle Stroke="{AppThemeBinding Light={StaticResource outlineVariant}, Dark={StaticResource secondaryContainerDark}}" />
                        </chart:NumericalAxis.MajorGridLineStyle>
                        <chart:NumericalAxis.MajorTickStyle>
                            <chart:ChartAxisTickStyle TickSize="0" />
                        </chart:NumericalAxis.MajorTickStyle>
                    </chart:NumericalAxis>
                </chart:SfCartesianChart.YAxes>

                <chart:ColumnSeries
                    Width="0.8"
                    Fill="{AppThemeBinding Light={StaticResource Green},
                                        Dark={StaticResource LightGreen}}"
                    ItemsSource="{Binding IncomeData}"
                    ShowDataLabels="True"
                    XBindingPath="Month"
                    YBindingPath="Amount">
                    <chart:ColumnSeries.DataLabelSettings>
                        <chart:CartesianDataLabelSettings LabelPlacement="Outer">
                            <chart:CartesianDataLabelSettings.LabelStyle>
                                <chart:ChartDataLabelStyle
                                    x:Name="IncExpChartIncLbl"
                                    Background="#00ffffff"
                                    FontSize="11"
                                    LabelFormat="$0"
                                    TextColor="{AppThemeBinding Light={StaticResource Green},
                                                                Dark={StaticResource LightGreen}}" />
                            </chart:CartesianDataLabelSettings.LabelStyle>
                        </chart:CartesianDataLabelSettings>
                    </chart:ColumnSeries.DataLabelSettings>
                </chart:ColumnSeries>
                <chart:ColumnSeries
                    Width="0.8"
                    Fill="{StaticResource Red}"
                    ItemsSource="{Binding ExpenseData}"
                    ShowDataLabels="True"
                    XBindingPath="Month"
                    YBindingPath="Amount">
                    <chart:ColumnSeries.DataLabelSettings>
                        <chart:CartesianDataLabelSettings LabelPlacement="Outer">
                            <chart:CartesianDataLabelSettings.LabelStyle>
                                <chart:ChartDataLabelStyle
                                    x:Name="IncExpChartExpLbl"
                                    Background="#00ffffff"
                                    FontSize="11"
                                    LabelFormat="$0"
                                    LabelPadding="5"
                                    TextColor="{AppThemeBinding Light={StaticResource Red},
                                                                Dark={StaticResource LightRed}}" />
                            </chart:CartesianDataLabelSettings.LabelStyle>
                        </chart:CartesianDataLabelSettings>
                    </chart:ColumnSeries.DataLabelSettings>
                </chart:ColumnSeries>
            </chart:SfCartesianChart>
        </Border>

        <Label
            Grid.Row="3"
            Margin="10,0,0,0"
            Style="{StaticResource lblTitle}"
            Text="Totals" />

        <VerticalStackLayout Grid.Row="4" Spacing="10">
            <Border Padding="5,5,10,5">
                <Grid ColumnDefinitions="40,*,*">
                    <Image
                        HeightRequest="40"
                        HorizontalOptions="Center"
                        Source="{FontImageSource FontFamily=MaterialRoundedFilled,
                                                Glyph={x:Static m:MaterialOutlined.Trending_up},
                                                Color={AppThemeBinding Light={StaticResource Green},
                                                                        Dark={StaticResource LightGreen}}}"
                        VerticalOptions="Center" />
                    <Label
                        Grid.Column="1"
                        Margin="5,0,0,0"
                        FontSize="18"
                        Text="Income"
                        VerticalOptions="Center" />
                    <Label
                        Grid.Column="2"
                        FontSize="18"
                        HorizontalOptions="End"
                        Text="{Binding TotalIncome, Converter={StaticResource ShowValueConverter}, StringFormat='{0:C2}'}"
                        VerticalOptions="Center" />
                </Grid>
            </Border>

            <Border Padding="5,5,10,5">
                <Grid ColumnDefinitions="40,*,*">
                    <Image
                        HeightRequest="40"
                        HorizontalOptions="Center"
                        Source="{FontImageSource FontFamily=MaterialRoundedFilled,
                                                Glyph={x:Static m:MaterialOutlined.Trending_down},
                                                Color={AppThemeBinding Light={StaticResource Red},
                                                                        Dark={StaticResource LightRed}}}"
                        VerticalOptions="Center" />
                    <Label
                        Grid.Column="1"
                        Margin="5,0,0,0"
                        FontSize="18"
                        Text="Expenses"
                        VerticalOptions="Center" />
                    <Label
                        Grid.Column="2"
                        FontSize="18"
                        HorizontalOptions="End"
                        Text="{Binding TotalExpense, Converter={StaticResource ShowValueConverter}, StringFormat='{0:C2}'}"
                        VerticalOptions="Center" />
                </Grid>
            </Border>
        </VerticalStackLayout>
    </Grid>
</components:CustomContentPage>