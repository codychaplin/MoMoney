<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MoMoney.Views.AddTransactionPage"
             NavigationPage.HasNavigationBar="False"
             Title="">

    <!-- page styles -->
    <ContentPage.Resources>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Padding" Value="10" />
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="TextColor" Value="White" />
            <Setter Property="BackgroundColor" Value="{StaticResource Gray900}" />
        </Style>
        <Style TargetType="{x:Type Image}" BasedOn="{x:StaticResource imgTinted}">
            <Setter Property="HeightRequest" Value="25" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="VerticalOptions" Value="Center" />
        </Style>
        <Style TargetType="{x:Type Frame}">
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="10,0,10,10" />
            <Setter Property="BackgroundColor" Value="{StaticResource Gray900}" />
        </Style>
    </ContentPage.Resources>
    
    <!-- main page -->
    <VerticalStackLayout>
        
        <!-- transaction type buttons -->
        <Frame Margin="10" Padding="2">
            <Grid x:Name="grdTransactionTypeButtons" Margin="1" ColumnDefinitions="*,*,*" ColumnSpacing="10">
                <Button x:Name="btnIncome" Grid.Column="0" Text="Income" Clicked="btnIncome_Clicked" />
                <Button x:Name="btnExpense" Grid.Column="1" Text="Expense" Clicked="btnExpense_Clicked" />
                <Button x:Name="btnTransfer" Grid.Column="2" Text="Transfer" Clicked="btnTransfer_Clicked" />
            </Grid>
        </Frame>
        <!-- Date -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="calendar.png" />
                <DatePicker x:Name="dtDate" Grid.Column="1" VerticalOptions="Center" />
            </Grid>
        </Frame>
        <!-- Account -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="credit_card.png" />
                <Picker x:Name="pckAccount" Grid.Column="1" Title="Select an Account">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Tan Check</x:String>
                            <x:String>Tan Savings</x:String>
                            <x:String>Tan TFSA</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </Grid>
        </Frame>
        <!-- Amount -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="dollar_symbol.png" />
                <Entry x:Name="txtAmount" Grid.Column="1" Placeholder="$0.00" />
            </Grid>
        </Frame>
        <!-- Category -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="category.png" />
                <Picker x:Name="pckCategory" Grid.Column="1" Title="Select a Category" />
            </Grid>
        </Frame>
        <!-- Subcategory -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="subcategory.png" />
                <Picker x:Name="pckSubcategory" Grid.Column="1" Title="Select a Subcategory" />
            </Grid>
        </Frame>
        <!-- Payee -->
        <Frame>
            <Grid ColumnDefinitions="0.15*,*">
                <Image Source="account.png" />
                <Entry x:Name="txtPayee" Grid.Column="1" Placeholder="Payee" />
            </Grid>
        </Frame>
        <!-- clear/enter buttons -->
        <Grid x:Name="grdAcceptButtons" Margin="10,0,10,10" Padding="2" ColumnDefinitions="*,*" ColumnSpacing="10">
            <Button x:Name="btnClear" Grid.Column="0" Text="Clear" BackgroundColor="{StaticResource Blue}" Clicked="btnClear_Clicked" />
            <Button x:Name="btnEnter" Grid.Column="1" Text="Accept" BackgroundColor="{StaticResource Green}" Clicked="btnEnter_Clicked" />
        </Grid>
    </VerticalStackLayout>
    
</ContentPage>